var Orbit=function(planet,moon,options){"use strict";var animationInterval,self=this,defaultAngle=options.startAngle||0;this.planet={el:planet,x:planet.offsetLeft,y:planet.offsetTop,center:planet.offsetHeight/2},this.moon={el:moon,center:moon.offsetHeight/2},this.distance=options.distance||10,this.speed=options.speed||1,this.refAngle=defaultAngle;var calculatePosition=function(){self.refAngle=self.refAngle<=defaultAngle+360?self.refAngle:defaultAngle;var refRadians=self.refAngle*(Math.PI/180),xPos=Math.round(Math.sin(refRadians)*self.distance),yPos=Math.round(Math.cos(refRadians)*self.distance);self.planet.x=self.planet.el.offsetLeft,self.planet.y=self.planet.el.offsetTop;var leftPos=Math.round(xPos+self.planet.x+self.planet.center-self.moon.center),topPos=Math.round(yPos+self.planet.y+self.planet.center-self.moon.center);self.moon.el.style.left=leftPos+"px",self.moon.el.style.top=topPos+"px",self.refAngle=self.refAngle+self.speed};return this.bigBang=function(){animationInterval=setInterval(calculatePosition,24)},this.step=function(){clearInterval(animationInterval),calculatePosition()},this.pause=function(){clearInterval(animationInterval)},{bigBang:this.bigBang,step:this.step,pause:this.pause}};